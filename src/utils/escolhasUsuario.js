const escolherDownload = (tipoArquivo) => {
    
    return tipoEscolhido
};

const escolherNome = (nomeArquivo) => {

    return nomeEscolhido
};

const escolherQualidade = (qualidadeDownload) => {
    
    return qualidadeEscolhida
};

module.exports = {
    escolherDownload,
    escolherNome,
    escolherQualidade,
};